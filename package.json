{
  "name": "civx-angular2-shared",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "tsc",
    "clean": "rm -f *d.ts src/*d.ts *.map src/*.map *.js src/*.js"
  },
  "author": "",
  "dependencies": {
    "@angular/core": "^2.1.2",
    "@angular/http": "^2.1.2",
    "rxjs": "^5.0.0-rc.2"
  },
  "devDependencies": {
    "@types/core-js": "^0.9.34",
    "typescript": "^2.0.9"
  }
}
